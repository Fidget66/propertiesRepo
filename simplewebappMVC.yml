server:
  port: 8088
  servlet:
    context-path: /webapp
eureka:
  instance:
    appname: webapp
  client:
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
